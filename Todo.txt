# Spajanje na wifi (ako je laptop)
	iwctl --passphrase PASSPHRASE station wlan0 connect SSID

# Download i pokretanje skripte
	curl -LO https://raw.githubusercontent.com/fpusticki1/arch/main/arch.sh
	bash arch.sh

---------------------------------------------
Configure VPN
Seahorse - set empty password
Wallpaper

Region and language
Formats
Keyboard shortcuts
-----------------------
TWEAKS:
	Interface Text	- Ubuntu Regular 11
	Document Text	- Sans Regular   11
	Monospace Text	- Hack Regular   11
	Legacy Window 	- Ubuntu Bold    11
	
EXTENSIONS:
	Gnome 40 UI Improvements
	No Overview at start-up
	Top Panel Workspace Scroll
	Unblank lock screen

------------------------
Sublime
	- Install compare plugin
		sbs_compare.py
			Add after line 570:
				new_window.set_view_index( new_window.active_view(), 0, 0 )
	- Add to settings
		"show_encoding": true,
		"fallback_encoding": "Central European (Windows 1250)"

Install Termius-app
	sudo mkdir -p /opt/Termius/chrome-sandbox
	sudo rm -rf /opt/Termius
	cd .cache/paru
	paru -G termius-app
	cd termius-app
	sudo nano PKGBUILD
		- change version and save (get deb version from official web)
	paru -Ui
	
Turn off startup applications
	delete templates folder
	delete public folder
	
Plank autostart, configure, theme

Configure apps

Nautilus settings
	as user:
		gsettings set org.gnome.nautilus.list-view default-visible-columns "['name','size','type','date_modified']"
		
	# local access to pc / laptop
	sftp://192.168.1.xxx/
	Mount disk, add to Nautilus bookmarks, standby after 5 seconds

/home/fpusticki/temp
  - Install Zoiper as root
  - paru -S zoiper-classic
  - mysql connections
  
PLEX
	http://127.0.0.1:32400/web
------------------------
alsamixer - add microphone sidetone
	sudo alsactl store
